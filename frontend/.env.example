# Full URL of the deployed backend API, including the /api path.
# Example: https://grit-to-grace-backend.vercel.app/api
# Leave empty for local development (uses the Vite dev server proxy to localhost:5000).
VITE_API_URL=
